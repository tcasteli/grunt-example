zip: {
    dev: {
        cwd: 'dist/',
        src: ['dist/**/*'],
        dest: 'dist/DevAppBuild.zip'
    },
    prd: {
        cwd: 'dist/',
        src: ['dist/**/*'],
        dest: 'dist/PrdAppBuild.zip'
    }
}